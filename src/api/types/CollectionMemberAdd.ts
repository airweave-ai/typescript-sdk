/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as AirweaveSDK from "../index.js";

/**
 * Schema for adding a member to a collection.
 */
export interface CollectionMemberAdd {
    /** Email address of the user to add to the collection. The user must be a member of the organization. */
    user_email: string;
    /**
     * Role to assign to the user in this collection:
     * • **collection_owner**: Full control over the collection (modify, delete, manage members)
     * • **collection_editor**: Can perform operations (refresh, sync) but cannot modify collection settings
     * • **collection_viewer**: Read-only access to the collection
     */
    role: AirweaveSDK.CollectionRole;
}
